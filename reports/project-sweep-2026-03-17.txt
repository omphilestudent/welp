WELP PROJECT SWEEP (2026-03-17)

Scope
- Codebase reviewed at a repo level: backend, frontend, docs, ml-services.
- Focused on mismatches, missing pieces, runtime errors reported in logs, and obvious debt.
- Not a security audit; not an exhaustive test run due to DB connectivity failures.

High Priority Issues (Fix First)
1) Flow creation fails due to DB constraint mismatch
   - Evidence: flows table enforces type IN ('screen','trigger') in backend/src/utils/database.js.
   - UI uses flow types like automation/scheduled/event; server also accepts more types in UI.
   - Action: either expand the DB constraint or map UI types to allowed values server-side.

2) Psychologist registration 500s caused by schema drift
   - Two different schemas exist for psychologist_applications.
   - backend/src/controllers/authController.js expects user_id-based schema.
   - backend/src/controllers/psychologistController.js creates an email/full_name schema.
   - Action: align on a single schema or maintain compatibility (current authController now supports legacy columns).

3) Admin settings write failures due to foreign-key updates (previously observed)
   - Error mentions system_settings.updated_by foreign key.
   - Action: ensure updated_by uses an existing user id or allow NULL when unauthenticated.

4) Ads admin list failures (500)
   - Previously observed: /ads/admin/list and /ads/admin/failures.
   - Action: verify SQL join assumptions, missing columns, or missing data in ad tables.

5) DB-dependent tests are blocked
   - Tests fail on Neon connection / DATABASE_URL.
   - Action: fix pooled connection string or provide a local test DB for CI/dev.

Medium Priority Issues
6) Duplicate/overlapping routes/controllers for psychologist applications
   - /auth/register/psychologist (authController) and /api/psychologists/apply (psychologistController).
   - Action: consolidate to one entry point or keep both but share a service layer.

7) Flow canvas model vs runtime execution mismatches
   - Frontend uses nodes + edges for graph; backend flow model uses type + definition JSON.
   - Action: define a shared schema and validate before save (start/end nodes, no cycles).

8) Admin UI warning: invalid DOM nesting
   - Warning in AdminDashboard: whitespace text nodes inside <tr>.
   - Action: remove whitespace or wrap in fragments without text nodes.

9) Business/Psychologist registration forms contain inline styling
   - Inline styles reduce maintainability; some sections not covered by CSS.
   - Action: move inline styles to Register.css for consistency.

Low Priority / Cleanup
10) Repository includes compiled frontend/dist assets
    - This bloats diffs and makes grep noisy.
    - Action: add to .gitignore if not required for deployment.

11) Logs: many “Migration note” warnings
    - Repeated warnings indicate repeated migration attempts or redundant checks.
    - Action: reduce noise and make migrations idempotent with clear logging.

Missing / Needs Adding (Recommendations)
12) End-to-end contract tests for admin endpoints
    - Especially flows, settings, ads, and registration endpoints.

13) Central API schema or OpenAPI definition
    - Prevents naming drift across frontend/backend.

14) Flow validator on backend
    - Enforce required start/end nodes and terminate all branches server-side.

15) Data migrations for legacy schema alignment
    - Psychologist applications and flow type check constraint are the clearest.

Detailed References
- Flow DB constraint: backend/src/utils/database.js (flows.type check).
- Psychologist registration schema paths: backend/src/controllers/authController.js, backend/src/controllers/psychologistController.js.
- Admin DOM nesting warning: frontend/src/pages/admin/AdminDashboard.jsx.
- Tests failing on DB: backend npm test output (DATABASE_URL/Neon).

Recommended Next Actions
1) Decide on flow type canonical list; add DB migration + server validation.
2) Remove or refactor psychologistController path; keep one schema.
3) Fix DB connection in .env and re-run blocked tests.
4) Add backend flow validation + shared schema.
5) Address admin DOM nesting warning.

Notes
- This report is based on code inspection and previous runtime logs. Some items require runtime verification after DB connectivity is restored.
